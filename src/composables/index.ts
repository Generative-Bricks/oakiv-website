/**
 * Vue Composables exports
 *
 * Note: Chat functionality has been consolidated into useChatStore (src/stores/chat.ts).
 * Use `import { useChatStore } from '@/stores/chat'` for all chat-related state and actions.
 */

// export { useServices } from './useServices'
// export { useScrollTo } from './useScrollTo'
